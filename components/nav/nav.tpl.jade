.ui.borderless.inverted.menu
    a.ui.header.item(href='/view/rzpRYuGK2Rwh2Cv9w' class="{{isActivePath '/view/rzpRYuGK2Rwh2Cv9w'}}") 
        .facet_black
            |F
    a.icon.item(href='/view/2knFbcAMjaCjPFuri' class="{{isActivePath '/view/2knFbcAMjaCjPFuri'}}") 
        i.large.clone.icon
    if isInRole 'admin'
        a#toggle_admin_mode.icon.item(href='#' data-position="bottom center" data-content="Toggle Admin Mode") 
            if admin_mode
                i.blue.protect.large.icon
            else
                i.protect.large.icon
        
    a.icon.item(href='/admin/content' class="{{isActivePath '/admin/'}}")
        i.configure.large.icon
        // | Admin
    a.icon.item(href='/messages' class="{{isActivePath '/messages'}}")
        i.mail.large.icon
    a.icon.item(href='/notifications' class="{{isActivePath '/notifications'}}")
        i.bell.large.icon
    if currentUser
        a.icon.item(href="/account/profile/edit" class="{{isActivePath '/account/profile/edit'}}")
            if currentUser.image_id
                img.ui.avatar.image(src="{{c.url currentUser.image_id width=50 height=50 gravity='face' crop='fill'}}")
            else
                i.user.large.icon
            // if currentUser.name
            //     | #{currentUser.name}
            // else
                // | Profile
        a.icon.item(href="/profile/#{currentUser.username}/account" class="{{isActivePath '/profile/#{currentUser.username}/account'}}")
            i.settings.large.icon
        a#logout.ui.icon.item(href='#') 
            i.sign.out.large.icon
            // |Sign Out
    else
        a.ui.icon.item(href='/sign-in') 
            i.sign.in.large.icon
            // |Sign In
